{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo 'no runtime dependencies'",
  "buildCommand": "node build/build.js --check && mkdir -p _site && cp index.html _site/",
  "outputDirectory": "_site"
}
