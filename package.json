{
  "name": "sync-deploy",
  "version": "1.0.0",
  "private": true,
  "description": "Sync — workflow intelligence. Deployed as a single portable index.html; this package exists only to run the UI/e2e suite.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
